global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  # Requires prom-client + /metrics endpoint in Express (add as needed)
  - job_name: "karmamap-backend"
    static_configs:
      - targets: ["backend:3001"]

  - job_name: "caddy"
    static_configs:
      - targets: ["caddy:2019"]
